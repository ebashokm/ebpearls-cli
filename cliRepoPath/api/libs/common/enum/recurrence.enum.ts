export enum repeatFrequencyEnum {
  day = 'day',
  week = 'week',
  month = 'month',
  year = 'year',
}

export enum endTypeEnum {
  never = 'never',
  date = 'date',
  occurrence = 'occurrence',
}

export enum clientClassBookingFrequency {
  definite = 'definite',
  indefinite = 'indefinite',
}

export enum ClassBookingOption {
  availableOnly = 'availableOnly',
  allEvenIfFull = 'allEvenIfFull',
  waitlistIfFull = 'waitlistIfFull',
}

export enum DiscountCodeDuration {
  once = 'once',
  indefinite = 'indefinite',
}
