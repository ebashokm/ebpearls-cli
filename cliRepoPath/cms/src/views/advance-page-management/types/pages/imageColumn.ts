export type mainFields = {
    title: {
        text: string;
    }[];
};

export type subFields = {
    id: string;
    icons: {
        image: string;
        align: string;
        main: string;
        sub: string;
    }[];
};
