export type mainFields = {
    title: {
        text: string;
    }[];
};

export type subFields = {
    id: string;
    customer: {
        comment: string;
        image: string;
        name: string;
        location: string;
    }[];
};

export type mainFieldsForPage = {
    heading: {
        text: string;
    }[];
};

export type mainFieldsForPage1 = {
    heading1: {
        text1: string;
    }[];
};
