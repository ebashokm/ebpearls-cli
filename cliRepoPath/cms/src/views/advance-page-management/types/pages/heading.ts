export type mainFields = {
    heading: {
        text: string;
    }[];
};
export type subFields = {
    id: string;
    listings: {
        image: string;
        alt: string;
        title: string;
        url: string;
    }[];
};
